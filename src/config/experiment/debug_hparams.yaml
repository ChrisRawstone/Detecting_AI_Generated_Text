model_settings:
  cls: 'distilbert-base-uncased' # model_type
  num_labels: 2

training_args:
  output_dir: './results'
  num_train_epochs: 3
  per_device_train_batch_size: 16
  per_device_eval_batch_size: 64
  warmup_steps: 5
  weight_decay: 0.01
  logging_dir: './logs'
  logging_steps: 1
